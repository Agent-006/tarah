import * as React from "react";

export function InsertImageDialog({
  activeEditor,
  onClose,
}: {
  activeEditor?: any;
  onClose?: () => void;
}) {
  // Minimal stub used during build/type checking. The real implementation
  // lives elsewhere or isn't needed for server-side builds.
  return null;
}
